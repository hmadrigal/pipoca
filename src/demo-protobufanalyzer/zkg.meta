[package]
credits = H. Madrigal <hfmad@hotmail.com>.
description = Prototype for a ProtocolBuf decoder.
tags = zeek plugin, file analyzer, gRPC, protobuf
script_dir = scripts
depends =
    zeek >=3.0.0
plugin_dir = build/Demo_ProtobufAnalyzer.tgz
build_command = ./configure && make
# ./configure --enable-debug && make clean && $(if [ -d ./build/ ]; then rm -rf ./build/; fi) && make && echo "Exit Code ${?}"
# test_command = make test
